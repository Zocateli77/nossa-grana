[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback — todas as rotas do React Router caem no index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
